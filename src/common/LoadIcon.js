import React from "react";
import "./LoadIcon.css";

/** Loading message used by components that fetch API data. */

function LoadIcon() {
  return <div className="LoadIcon">Loading ...</div>;
}

export default LoadIcon;
